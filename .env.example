# Copie este arquivo para .env (que está no .gitignore) e preencha as chaves.
# .env NUNCA deve ser commitado.

# Google Gemini — geração de imagens via Nano Banana (gemini-2.5-flash-image)
# Obtenha em: https://aistudio.google.com/apikey
GEMINI_API_KEY=
